spring:
  sql:
    init:
      mode: always
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
    locations: classpath:db/migration-test



brasil-api:
  base-url: http://localhost:${wiremock.server.port:5000}/api
  cep-uri: /cep/v1/{cep}
